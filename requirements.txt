# Core: the LLM clients. Everything talks to a model through afb/llm.py.
anthropic>=0.40            # Claude backend           (AFB_PROVIDER=anthropic)
openai>=1.0               # OpenAI-compatible backend (AFB_PROVIDER=openai):
                          #   Ollama / vLLM / LM Studio / llama.cpp / LocalAI

# Loads ANTHROPIC_API_KEY / TELEGRAM_BOT_TOKEN from a .env file.
python-dotenv>=1.0

# Research agent: real (keyless) web search + page fetching.
ddgs>=6.0
requests>=2.31

# Phone control. Only needed for `python run.py telegram`.
python-telegram-bot>=20.0
